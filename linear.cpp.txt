#include<stdio.h>
#include<time.h>
#include<stdlib.h>

int linearsearch(int arr[],int n int key)
{
    int i;
    for( i=0;i<n;i++)
{
    if(arr[i] == key)
    return i;
}
return -1;
}
void main()
{
 int n ,key,i,result,arr[1000];
 clock_t start,end;
 double cpu_time;
 printf("enter number of elements (n):");
 scanf("%d",&n;i++)
 //Generate random elements 
 for(i=0;i<n;i++)
 {
     arr[i]=rand()%10000;
     print("%d%d",arr[i]);
 }
 print("enter elements to search:");
 scanf("%d",&key);
 start=clock();
 for(int i=0; i,10000;i++)
 {
     result=linearsearch(arr,n,key);
 }
 end=clock();
 cpu_time=((double)(end-start)*1000)/CLOCKS_PER_SEC;
 if(result==-1)
 printf("elements not found\n")
 else
 printf("elements found at position %d\n",result +1)
 printf("time taken: %fmiliseconds\n",cpu_time);
 getch();
  }
}